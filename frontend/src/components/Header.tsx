import React from "react";
import { AppBar, Toolbar } from "@mui/material";

const Header = () => {
  return (
    <AppBar sx={{}}>
      <Toolbar></Toolbar>
    </AppBar>
  );
};

export default Header;
